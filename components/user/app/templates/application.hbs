<nav class="top-bar" data-topbar role="navigation">
    <ul class="title-area">
        <li class="name">
            <h1>
                <a href="/user">
                    <!-- TODO: show the user icon if logged in -->
                    <i class="fa fa-user"></i>
                    <!--<i class="fa fa-sign-in"></i>-->
                    <!--<i class="fa fa-ban"></i>-->
                    <!--<div class="fa fa-minus-o"></div>-->
                    <!--User-->
                </a>
            </h1>
        </li>
        <li class="toggle-topbar menu-icon"><a href="#">Menu</a></li>
    </ul>
    <section class="top-bar-section">
        <ul class="right">
            {{#if loggedIn}}
            <li class="has-dropdown not-click">
                <a href="#">
                    <img class="profile-photo-small" {{bind-attr src=gravatarUrl}}>
                </a>
                <ul class="dropdown">
                    <li>{{#link-to 'profile.edit'}}Profile Edit{{/link-to}}</li>
                    <li>{{#link-to 'profile.view'}}Profile View{{/link-to}}</li>
                    <li> <a href="#" {{action 'logout'}}>Log Out</a> </li>
                </ul>
            </li>
            {{else}}
                <li>
                    {{#link-to 'login'}}
                        <i class="fa fa-sign-in" title="Log In"></i>
                    {{/link-to}}
                </li>
            {{/if}}
        </ul>
    </section>
</nav>
{{outlet}}
