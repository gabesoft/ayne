<div class="grid-frame">
    <div class="grid-block">
        <div class="grid-content">
            <ul class="side-nav">
                {{#each url in model}}
                    <li>
                        {{render 'urlview' url}}
                    </li>
                {{/each}}
            </ul>
        </div>
    </div>
</div>
