<div class="url-view">
    <a href={{model.href}} class="url-title" target="_blank">
        {{view 'favicon' src=model.favicon }}
        {{model.title}}
    </a>
    {{#if view.showPopup}}
        {{render 'urlpopup' model}}
    {{/if}}
</div>
